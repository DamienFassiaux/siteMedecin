{% extends 'base.html.twig' %}

{% block body %}

 <h1> Recherche avancée </h1>

         {{ form_start(formSearch, {'attr': {'class': 'col-md-5 mx-auto'}}) }}

         {{ form_row(formSearch.specialite) }}

         {{ form_row(formSearch.departement) }}

         <button type="submit" class="btn btn-secondary mb-5">Filtrer</button>
  

          {{ form_end(formSearch) }}


  {#} <h1 class="display-4 text-center my-5"> {{ title }}</h1>#}


      <div class="row">

      {% for data in medecins %}

      {# {{ dump(data)}} #}
        
     

          <div class="col-md-4">
              <div class="card mb-4 shadow-sm">
               {#<img src="{{ asset('uploads/images/' ~ data.image) }}" alt="{{data.title}} ">#}

                <div class="card-body">

                  <h4 class="text-center"> {{data.nom}} </h4> <hr>

                  <p class="card-text">{{ data.ville }}</p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group mr-2">
                      <a href="{{ path('site_medecin', {'id': data.id}) }}" class="btn btn-sm btn-outline-secondary">En savoir plus</a>
            
                    </div> 
                  </div>
                </div>
              </div>
            </div>

      {% endfor %} 

      </div>
{% endblock %}



