{# {% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
 <div class="container_1">

     <main class="main">
         <div class="carousel-slide">
          <img id="carou_imgun "src="img/imgdoc3.jpg" alt="">
          <img id="carou_imgdeux "src="img/imgdoc2.jpg" alt="">
          <img id="carou_imgtrois "src="img/imgdoc1.jpg" alt="">

         </div>

     </main>

 </div>

 <div class="container_2">
    <section class="section_1">

    </section>
 </div>

{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Liste des médecins{% endblock %}

{% block body %}

<h1 class="display-4 text-center my-5"> {{ title }}</h1>


      <div class="row">

      {% for data in medecins %}

      {# {{ dump(data)}} #}
        
     

            <div class="col-md-4">
              <div class="card mb-4 shadow-sm">
               {#<img src="{{ asset('uploads/images/' ~ data.image) }}" alt="{{data.title}} ">#}

                <div class="card-body">

                  <h4 class="text-center"> {{data.nom}} </h4> <hr>

                  <p class="card-text">{{ data.ville }}</p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group mr-2">
                      <a href="{{ path('site_medecin', {'id': data.id}) }}" class="btn btn-sm btn-outline-secondary">En savoir plus</a>
            
                    </div> 
                  </div>
                </div>
              </div>
            </div>

      {% endfor %}

      </div>

{% endblock %}
