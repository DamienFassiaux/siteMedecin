{% extends 'home.html.twig' %}

{% block title %}Bienvenue dans le BackOffice!{% endblock %}

{% block body %}

  <div class="container">
     
       <h1 class="display-4 text-center my-5">BACKOFFICE</h1>

       <h4 class="text-center mb-5"> Bienvenue 

       {% if app.user %}

          <span class="text-info">{{ app.user.nom }}</span>

        {% endif %}

        dans votre espace personnalisé
        </h4>
        <div class="text-center">

        <a href="{{ path('admin_medecins') }}" class="col-md-10 btn btn-primary mb-4">GESTION DES MEDECINS</a>

        <a href="{{ path('admin_users') }}" class="col-md-10 btn btn-info mb-4">GESTION DES PATIENTS</a>

        <a href="{{ path('admin_departements') }}" class="col-md-10 btn btn-primary mb-4">GESTION DES DEPARTEMENTS</a>

        <a href="{{ path('admin_specialites') }}" class="col-md-10 btn btn-info mb-4">GESTION DES SPECIALITES </a>

        <a href="{{ path('admin_avis') }}" class="col-md-10 btn btn-primary mb-4">GESTION DES AVIS </a>

       </div>
    </div>

{% endblock %}
