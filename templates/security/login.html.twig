{% extends "base.html.twig" %}

{% block title %}
	Identifiez-vous
{% endblock %}

{% block body %}


<div class="big_container_connexion"> 
<div class="filter_blur"></div>   
 <div class="container_connexion">

  <div class="connexion"> 
   <div class="logo_connexion"></div>
   <h1 class="display-4 text-center my-5 titre1_connexion"> Identifiez-vous</h1>

  <!-- {% for message in app.flashes('success') %}
     <div class="col-md-3 bg-success p-4 rounded mx-auto text-center text-white mb-3">
     {{ message }}
     </div>
     {% endfor %}
     {% if error %}
     <div class="col-md-3 bg-danger p-4 rounded mx-auto text-center text-white mb-3">
      {{error.messageKey|trans(error.messageData, 'security') }}    
     </div>
       {% endif %} -->


     <form method="post" class="col-md-5 mx-auto formulaire_connexion" action="{{ path('security_login') }}">

            <div class="form-group email_connexion">      
                <input class="email_connexion_input" placeholder="Adresse email" type="email" class="form-control" id="_username" name="_username" value="{{ lastUsername }}">
            </div>


            <div class="form-group password_connexion">                
                <input class="password_connexion_input" placeholder="Mot de passe" type="password" class="form-control" id="_password" name="_password">

            </div>
        
            <button type="submit" class=" bcontinuer">Continuer</button>
     </form>
   </div>

  </div>

 </div> 
{% endblock %}
